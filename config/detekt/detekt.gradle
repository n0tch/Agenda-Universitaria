apply plugin: 'io.gitlab.arturbosch.detekt'

detekt {
    toolVersion = "1.23.1"
    config.setFrom(file("$rootDir/config/detekt/detekt.yml"))
    reports {
        html.enabled = true
    }
    parallel = true
    autoCorrect = true
}
